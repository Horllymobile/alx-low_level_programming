#include "main.h"

/**
 * rev_string - a function that reverse a string
 * @s: parameter to be reversed
 * Return: void
 **/

void rev_string(char *s)
{
  char *temp;
  int len;

  len = strlen(s);
}
